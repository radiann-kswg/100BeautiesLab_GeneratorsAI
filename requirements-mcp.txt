# MCP サーバ用依存（ベース依存 + MCP + 任意の出力シンク）
-r requirements.txt

# MCP Python SDK（FastMCP を含む）
# 2026-08-02: mcp 2.0.0 で `mcp.server.fastmcp` が削除され Cloud Run 起動時に
# ModuleNotFoundError → コンテナ即終了となったため、1.x 系へ上限ピン。
# 2.x へ上げる場合は src/mcp_server/ の FastMCP 依存の移行が必要。
mcp>=1.2.0,<2

# ── 出力シンク（使うものだけ入れれば良い） ───────────────────────
# OUTPUT_SINK=drive のとき
google-api-python-client>=2.100.0
google-auth>=2.23.0
# OUTPUT_SINK=gcs のとき
google-cloud-storage>=2.10.0
# Canva トークン永続化 (numbertales_refresh_canva_token ツール)
google-cloud-secret-manager>=2.16.0
